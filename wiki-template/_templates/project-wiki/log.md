# Project Wiki Log

Chronological record of wiki activity for this project. Append-only.

Format: `## [YYYY-MM-DD] <type> | <one-line summary>`
where `<type>` is one of: `meta`, `ingest`, `query`, `session`, `lint`, `promote`.
