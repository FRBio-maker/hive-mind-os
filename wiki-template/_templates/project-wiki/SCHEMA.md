# Project Wiki Schema

This wiki follows the global LLM Wiki protocol defined at `<vault>/SCHEMA.md`. **Read that document first** — it covers node format, types, edges, traversal, and workflows. This file contains only project-specific overrides.

## Project-specific overrides

*None yet. Add overrides below as the project develops.*

Examples of things that go here as the project matures:

- **Custom node types** declared via `type: custom`. State the type name, when to use it, and any frontmatter fields specific to it.
- **Project-specific tags** worth standardizing (e.g. `pcb-rev1`, `firmware-bootloader`, `field-test-q3`).
- **Naming conventions** unique to this codebase or project context.
- **Any deliberate deviation** from the global protocol — explain *why*, so future agents don't "fix" the deviation.

## Project context

(Optional: a short paragraph stating what this project is, so agents reading this wiki for the first time orient quickly. Hardware, codebase, goals.)
